apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "calert.fullname" . }}-test-connection"
  labels:
{{ include "calert.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "calert.fullname" . }}:{{ .Values.calert.service.port }}']
  restartPolicy: Never
